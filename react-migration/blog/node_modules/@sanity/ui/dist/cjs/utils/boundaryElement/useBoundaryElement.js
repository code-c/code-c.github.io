"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useBoundaryElement = void 0;
var react_1 = require("react");
var isRecord_1 = require("../../lib/isRecord");
var boundaryElementContext_1 = require("./boundaryElementContext");
var DEFAULT_VALUE = {
    version: 0.0,
    element: null,
};
function useBoundaryElement() {
    var value = react_1.useContext(boundaryElementContext_1.BoundaryElementContext);
    // NOTE: This check is for future-compatiblity
    // - If the value is not an object, it’s not compatible with the current version
    // - If the value is an object, but doesn’t have `version: 0.0`, it’s not compatible with the current version
    if (value && (!isRecord_1.isRecord(value) || value.version !== 0.0)) {
        throw new Error('useBoundaryElement(): the context value is not compatible');
    }
    return value || DEFAULT_VALUE;
}
exports.useBoundaryElement = useBoundaryElement;
//# sourceMappingURL=useBoundaryElement.js.map