var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { forwardRef } from 'react';
import styled from 'styled-components';
import { responsiveRadiusStyle } from '../../styles/internal';
import { Box } from '../box';
import { Label } from '../label';
import { badgeStyle } from './styles';
const Root = styled(Box)(responsiveRadiusStyle, badgeStyle);
export const Badge = forwardRef((props, ref) => {
    const { children, fontSize, mode = 'default', padding = 1, radius = 2, tone = 'default' } = props, restProps = __rest(props, ["children", "fontSize", "mode", "padding", "radius", "tone"]);
    return (React.createElement(Root, Object.assign({ "data-ui": "Badge" }, restProps, { "$mode": mode, "$tone": tone, "$radius": radius, padding: padding, ref: ref }),
        React.createElement(Label, { size: fontSize }, children)));
});
Badge.displayName = 'Badge';
//# sourceMappingURL=badge.js.map