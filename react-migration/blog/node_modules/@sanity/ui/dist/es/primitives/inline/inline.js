var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { forwardRef } from 'react';
import styled from 'styled-components';
import { Box } from '../box';
import { childrenToElementArray } from '../helpers';
import { inlineBaseStyle, inlineSpaceStyle } from './styles';
const Root = styled(Box)(inlineBaseStyle, inlineSpaceStyle);
export const Inline = forwardRef((props, ref) => {
    const { as, children: childrenProp, space } = props, restProps = __rest(props, ["as", "children", "space"]);
    const children = childrenToElementArray(childrenProp).filter(Boolean);
    return (React.createElement(Root, Object.assign({ "data-ui": "Inline" }, restProps, { "$space": space, forwardedAs: as, ref: ref }), children.map((child, idx) => (React.createElement("div", { key: idx }, child)))));
});
Inline.displayName = 'Inline';
//# sourceMappingURL=inline.js.map