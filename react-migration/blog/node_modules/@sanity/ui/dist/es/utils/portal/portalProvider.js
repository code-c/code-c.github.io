import React, { useMemo } from 'react';
import { PortalContext } from './portalContext';
const __BROWSER__ = typeof window !== 'undefined';
export function PortalProvider(props) {
    const { boundaryElement, children, element } = props;
    const value = useMemo(() => {
        return {
            version: 0.0,
            boundaryElement: boundaryElement || null,
            element: element || (__BROWSER__ && document.body) || null,
        };
    }, [boundaryElement, element]);
    return React.createElement(PortalContext.Provider, { value: value }, children);
}
//# sourceMappingURL=portalProvider.js.map