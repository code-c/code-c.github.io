import React, { useMemo } from 'react';
import { BoundaryElementContext } from './boundaryElementContext';
export function BoundaryElementProvider({ children, element, }) {
    const value = useMemo(() => ({ version: 0.0, element }), [element]);
    return React.createElement(BoundaryElementContext.Provider, { value: value }, children);
}
//# sourceMappingURL=boundaryElementProvider.js.map