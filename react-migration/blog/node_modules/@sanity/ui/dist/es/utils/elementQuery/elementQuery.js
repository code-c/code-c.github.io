var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { forwardRef, useEffect, useMemo, useState } from 'react';
import { useForwardedRef } from '../../hooks';
import { useTheme } from '../../theme';
import { ResizeObserver } from '../resizeObserver';
import { findMaxBreakpoints, findMinBreakpoints } from './helpers';
export const ElementQuery = forwardRef(function ElementQuery(props, ref) {
    const theme = useTheme();
    const { media } = theme.sanity;
    const { children } = props, restProps = __rest(props, ["children"]);
    const [width, setWidth] = useState(() => window.innerWidth);
    const forwardedRef = useForwardedRef(ref);
    useEffect(() => {
        let ro;
        if (forwardedRef.current) {
            const handleResizeEntries = (entries) => {
                setWidth(entries[0].contentRect.width);
            };
            ro = new ResizeObserver(handleResizeEntries);
            ro.observe(forwardedRef.current);
        }
        return () => {
            if (ro)
                ro.disconnect();
        };
    }, [forwardedRef]);
    const max = useMemo(() => findMaxBreakpoints(media, width), [media, width]);
    const min = useMemo(() => findMinBreakpoints(media, width), [media, width]);
    return (React.createElement("div", Object.assign({}, restProps, { "data-eq-max": max.length ? max.join(' ') : undefined, "data-eq-min": min.length ? min.join(' ') : undefined, ref: forwardedRef }), children));
});
//# sourceMappingURL=elementQuery.js.map